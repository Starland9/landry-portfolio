{
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Exécuter le script : dev",
      "request": "launch",
      "command": "pnpm run dev",
      "cwd": "${workspaceFolder}"
    }
  ]
}
